// @flow
import styled from 'styled-components';
import { modularScale } from 'polished';

const ArticleHeader = styled.header`
  margin-bottom: ${modularScale(-1)};
`;

const ArticleFooter = styled.footer``;

export { ArticleHeader, ArticleFooter };
